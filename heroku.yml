setup:
  addons:
    - plan: jawsdb-maria
      as: DATABASE
build:
  docker:
    web: Dockerfile
release:
  image: web
  command:
    - "php bin/console doctrine:mongodb:schema:update -n"
    - "php bin/console doctrine:mongodb:fixtures:load -n"
