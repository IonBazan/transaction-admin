{% extends 'admin.html.twig' %}

{% block title %}{% if transaction|default %}Edit transaction: {{ transaction.id }}{% else %}New Transaction{% endif %}{% endblock %}

{% block content %}
    {{ form_start(form) }}
    {{ form_errors(form) }}
    {{ form_row(form.amount.amount) }}
    {{ form_row(form.amount.currency) }}
    {{ form_end(form) }}
{% endblock %}
